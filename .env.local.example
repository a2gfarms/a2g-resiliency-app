# Air2Ground Resilient — environment template
# Copy to .env.local and fill in. .env.local is gitignored — never commit it.

# Stage 2 — Supabase (new, separate project — NOT the Meats project)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stage 5 — Anthropic (the coach)
ANTHROPIC_API_KEY=

# Stage 7 — Stripe (deferred until founding-member launch)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_FOUNDING_MONTHLY=
STRIPE_PRICE_FOUNDING_ANNUAL=
